{
  "dependencies": {
    "@playwright/test": "^1.36.1",
    "playwright": "^1.36.1"
  },

  "devDependencies": {
    "typescript": "^5.1.6",
    "ts-node": "^10.9.1"
  },

  "scripts": {
    "test": "npm run build && npm run test:run",
    "build": "tsc",
    "test:run": "ts-node *"
  }
}